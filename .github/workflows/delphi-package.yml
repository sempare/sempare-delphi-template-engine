name: Delphi package

on: [push, pull_request]

jobs:
  build_sempare_template_engine:
    runs-on: self-hosted
    strategy:
      fail-fast: true
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: build win32 debug
        shell: cmd
        id: build_win32_debug
        run: |
          rsvars.bat
          C:\windows\Microsoft.NET\Framework\v4.0.30319\msbuild Sempare.Template.Tester.dproj /t:Build /p:Config=Debug /p:Platform=Win32
      - name: build win32 release
        shell: cmd
        id: build_win32_release
        run: |
          rsvars.bat
          C:\windows\Microsoft.NET\Framework\v4.0.30319\msbuild Sempare.Template.Tester.dproj /t:Build /p:Config=Release /p:Platform=Win32

